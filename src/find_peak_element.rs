/// 162.寻找峰值
pub fn find_peak_element(nums: Vec<i32>) -> i32 {

  let mut left_index = 0;
  let mut right_index = 0;

  while left_index < right_index {
    
  }

  0
}

#[test]
fn test() {
    assert_eq!(find_peak_element(vec![1, 2, 3, 1]), 2);
}

#[test]
fn test_1() {
    assert_eq!(find_peak_element(vec![1, 2, 1, 3, 5, 6, 4]), 2);
}
