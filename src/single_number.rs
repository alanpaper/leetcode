/// 136. 只出现一次的数字
///

pub fn single_number(nums: Vec<i32>) -> i32 { 

    let mut sigle  = nums[0];

    for num in nums {
      
    }
    0
}

#[test]
fn test_1() {
    assert_eq!(single_number(vec![2, 2, 1]), 1)
}
